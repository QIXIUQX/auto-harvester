# 更新日志

## 0.0.3

### 适配

- **升级到 Minecraft 26.3**（Wilderness Bound 版本）：`fabric.mod.json` 的依赖同步改为 `~26.3`
- **Fabric API 升级到 0.160.6+26.3**；Fabric Loader 仍为 0.19.5（26.3 当前的最新稳定版）
- **构建工具链按官方建议跟进**：Loom 由 `1.17-SNAPSHOT` 固定为 `1.17.21`，Gradle 由 9.5.1 升到 9.6.0
- **适配 26.3 移除的方块 codec API**：原版在 26.3 中删除了 `BlockBehaviour#codec`、`simpleCodec` 以及各方块的 `CODEC` 字段，方块不再通过 `MapCodec` 序列化；本模组相应移除了 `AutoHarvesterBlock` 里的 `CODEC` 与 `codec()` 覆写（方块功能与存档数据不受影响）

本次为版本适配更新，模组功能没有变化。

## 0.0.2

### 新增

- **独立的创造模式分类**：创造模式物品栏里多了一个「自动收割机」分类，方块只放在这里，不再散落在各个原版分类中
- **左右两侧也能放箱子了**：现在会同时查看背面、左侧和右侧的箱子，依次存放；一个装满了自动用下一个，三个都满了才会掉在地上
- **两个新开关**（界面右上角）：
  - **满箱停收**（默认开）：箱子放不下时自动暂停收割，作物留在原地不会浪费，等你腾出空间后会自动继续
  - **静音**（默认关）：觉得收获提示音吵可以关掉
- **滚动条**：作物列表右侧加了滚动条，可以拖动或点击跳转，滚轮照旧可用
- **跟随游戏语言**：物品名和界面文字会跟随语言设置切换（目前支持中文与英文）
- **新增 3 种作物**：西瓜、南瓜、甜浆果，现在一共支持 10 种

### 修复

- 修好了方块挖掉后**什么都不掉**的问题：
  - 用石镐及以上的镐子挖掘，正常掉落方块本身
  - 空手、木镐、金镐或其它工具挖掘，只会把方块摧毁，不掉落任何东西
- 修好了打开设置界面时**整个世界停住**的问题（箱子盖卡在半空、生物不动、收割机也不工作）
- 修好了按 ESC 关闭设置界面时**游戏崩溃**的问题
- 修好了作物名显示成**一串字母**而不是名字的问题
- 修好了英文界面下标题和按钮文字**重叠、超出面板**的问题

### 优化

- **界面换成原版风格**：面板、格子和滚动条都改成 Minecraft 原版容器界面的样子，看起来更融入游戏
- 每个作物前面多了一个格子用来放它的图标，更直观
- 作物名称改用游戏自带的名称（例如「小麦作物」「甜浆果丛」）

## 0.0.1

- 初始版本：自动收割机方块，每秒收割前方 9×9 区域里成熟的作物，并自动补种
- 支持 7 种作物：小麦、胡萝卜、马铃薯、甜菜根、下界疣、火把花、瓶子草植株
- 右键打开界面，可单独开关每种作物
- 收成会存入方块后方的箱子，没有箱子时掉在地上
